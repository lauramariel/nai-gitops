apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nai-db
  namespace: nai-db
spec:
  instances: 1

  storage:
    size: 10Gi

  postgresql:
    parameters:
      max_connections: "1000"

  monitoring:
    enablePodMonitor: true
