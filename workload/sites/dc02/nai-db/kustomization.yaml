apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - cnpg-cluster.yaml
  - ../../../templates/nai-db

patches:
  - path: patch.yaml
    target:
      group: postgresql.cnpg.io
      version: v1
      kind: Cluster
      name: nai-db
      namespace: nai-db
